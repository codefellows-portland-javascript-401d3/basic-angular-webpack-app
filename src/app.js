import '../app/css/main.css';
import angular from 'angular';

const app = angular.module('kateGreeting', []);

app.controller('main', function() { });

//////////////////////////////////////////////////////////////

function allFilled () {
  // this function will check and see if all inputs are filled, if so the results will display, if not then no results
}

allFilled();
